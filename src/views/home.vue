<template>
    <div class="home-container">
        <div class="content">
            <div class="text">
                <h2 class="title">
                一次温暖的对话<br/>
                <span class="highlight-text">化孤独为慰籍</span>
                </h2>
            <p class="description">每个深夜，每个焦虑的时刻，我们都在这里，不必独自承受，让心与心的连接温暖您的每一天</p>
            <div class="hero-actions">
                <!-- 添加 @click 事件 -->
                <el-button size="large" @click="goToConsultation">开始倾述，获得陪伴</el-button>
                <el-button size="large" style="border-color: #fff" color="transparent" @click="goToEmotionDiary">记录心情，释放情感</el-button>
            </div>
            </div>
            <div class="robot">
                <el-image style="width: 150px; height: 150px" :src="iconUrl" />
            </div>
        </div>
    </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router' // 引入 useRouter

const router = useRouter() // 获取路由实例
const iconUrl = new URL('@/assets/images/robot-fill.png', import.meta.url).href

// 跳转到咨询页面
const goToConsultation = () => {
    router.push('/consultation')
}

// 跳转到情绪日志页面
const goToEmotionDiary = () => {
    router.push('/emotion-diary')
}
</script>

<style scoped lang="scss">
.home-container {
    /* --- 核心修复：突破父容器限制撑满全屏 --- */
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    
    /* 抵消 FrontendLayout 中 .main-container 自带的上下 20px padding */
    margin-top: -20px;
    margin-bottom: -20px;
    
    /* 高度微调：减去导航栏和页脚的大致高度，确保不出现多余滚动条 */
    min-height: calc(100vh - 131px); 
    /* ---------------------------------------- */

    background: linear-gradient(90deg, rgb(74, 156, 140) 0%, rgb(61, 138, 122) 100%) rgba(74, 156, 140, 0.95);
    color: white;
    padding: 5rem 0;
    display: flex;
    align-items: center;
    justify-content: center;
    
    .content {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 30px;
        .text {
            width: 500px;
            .title {
                font-size: 45px;
                font-weight: bold;
                margin-bottom: 15px;
                .highlight-text {
                    color: #ffd700;
                }
            }
            .hero-actions {
                margin-top: 30px;
                /* 给按钮增加一点间距 */
                display: flex;
                gap: 15px;
            }
        }
        .robot {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 260px;
            height: 260px;
            border-radius: 50%;
            border: 2px solid rgba(255, 255, 255, 0.2);
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.05) 100%);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1),inset 0 1px 0 rgba(255, 255, 255, 0.3);
        }
    }
}
</style>